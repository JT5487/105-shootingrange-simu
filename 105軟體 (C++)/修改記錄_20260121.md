# 105軟體 (C++) 修改記錄 - 2026年1月21日

## 修改目標
1. 六隻槍的選擇要可見（下拉選單選項可見）
2. 不同距離的調整設定功能（每把槍在不同距離有不同的歸零值）
3. 每一隻槍的強度設定
4. 第六隻槍的設定與其它不一樣（信號弱，需要特別處理）

---

## 修改時間線

### 18:00 - 18:17 | 初始功能開發
**目標：** 實現每槍最低強度門檻功能

**修改文件：**
- `index.html` - 前端 JavaScript 和 UI
- `cpp/include/IRTracker.hpp` - C++ 頭文件
- `cpp/src/IRTracker.cpp` - C++ 實現
- `cpp/src/main.cpp` - 命令處理

**具體修改：**
1. 在 `state` 對象添加 `shooterMinIntensity` 狀態
2. 添加 `updateShooterMinIntensity()` 函數
3. 在 `onShooterSelectChange()` 中載入最低強度門檻
4. 在 `loadShooterZeroingFromLocalStorage()` 中載入最低強度門檻
5. 後端添加 `setShooterMinIntensity()` 方法
6. 後端添加 `set_shooter_min_intensity` 命令處理
7. 在 `processingLoop()` 中添加強度過濾邏輯

### 18:17 - 18:39 | 編譯與預設值調整
**問題：** 編譯環境路徑問題、預設值調整

**修改：**
- 1-5號槍預設最低強度門檻：50 → 30 → 0
- 6號槍預設最低強度門檻：0（接受弱信號）
- 多次重新編譯 `t91_tracker.exe`

### 18:46 - 18:53 | 預設值問題
**問題：** 預設值太高導致抓不到信號

**修改：**
- 所有槍的最低強度門檻預設改為 0

### 18:53 - 18:59 | 功能異常，嘗試恢復
**問題：** 彈著點、特效、聲音都不見了

**操作：**
- 從備份 `105軟體 (C++)0121` 恢復文件
- 恢復後發現版本太舊，功能更少

### 19:09 - 19:16 | 從壓縮包恢復
**操作：**
- 從 `105軟體 (C++)-20260106.zip` 解壓恢復
- 恢復到 20251222 版本

**恢復文件：**
- `index.html`
- `training_screen_a.html`
- `training_screen_b.html`
- `t91_tracker.exe`
- `cpp/src/main.cpp`
- `cpp/src/IRTracker.cpp`
- `cpp/include/IRTracker.hpp`

### 19:16 - 19:30 | 重新添加功能
**問題：** 恢復的版本缺少所需功能

**重新添加的功能：**

1. **六隻槍選擇可見** (CSS 修復)
   - 添加 `select option` 樣式
   - 設定背景色 `#1a1a2e`、文字色 `#00d4ff`

2. **不同距離歸零設定**
   - 在 `state` 添加 `shooterZeroing` 結構（6槍 × 4距離）
   - 修改 `onShooterSelectChange()` 載入當前距離的歸零值
   - 修改 `updateShooterZeroing()` 保存當前距離的歸零值
   - 添加「目前編輯距離」顯示
   - 修改 `setShootingMode()` 切換距離時更新歸零顯示

3. **每槍最低強度門檻**
   - 在 `state` 添加 `shooterMinIntensity`
   - 添加 `updateShooterMinIntensity()` 函數
   - 添加 `loadShooterSettingsFromLocalStorage()` 函數
   - 添加最低強度門檻滑桿 UI (0-500)

4. **localStorage 持久化**
   - 歸零設定自動保存/載入
   - 最低強度門檻自動保存/載入

---

## 目前狀態

### 前端 (index.html) 已完成：
- [x] 六隻槍選擇可見
- [x] 不同距離歸零設定 UI
- [x] 最低強度門檻 UI
- [x] localStorage 持久化

### 後端 (C++) 待確認：
- [ ] `set_shooter_min_intensity` 命令處理（需確認是否在恢復的版本中）
- [ ] 強度過濾邏輯（需確認是否在恢復的版本中）

---

## 待測試項目
1. 六隻槍選擇是否可見
2. 切換距離後歸零值是否正確載入
3. 彈著點特效是否正常顯示
4. 聲音是否正常播放
5. 最低強度門檻功能是否正常

---

## 注意事項
- 第六隻槍信號弱，最低強度門檻應設為 0
- 其他槍如有雙擊誤判，可調高最低強度門檻
- 後端 C++ 可能需要重新編譯以支援最低強度門檻功能
