# 射擊訓練系統 - 發展工作註記 (截至 2025/12/31)

### 1. 靶位邏輯優化
*   **命中判定與狀態聯動**：實現只有在「立靶」狀態下才記錄擊中數。若靶位已為「倒靶」狀態（Turning target），則不計分。
*   **按鈕動態轉變**：
    *   每個射手區配置一組控制按鈕。
    *   立靶狀態時：按鈕顯示為紅色「倒靶」。點擊後執行倒靶指令，靶位轉向 90 度，按鈕轉為綠色「起靶」。
    *   倒靶狀態時：按鈕顯示為綠色「起靶」。點擊後執行起靶指令，靶位恢復正面，按鈕轉為紅色「倒靶」。

### 2. 介面佈局優化 (全螢幕無捲軸設計)
*   **射手卡片**：結構調整為「姓名區(上) + 主內容(中) + 動作按鈕(下)」。主內容採水平佈局，靶位圖像居左，分數(放大至 5em)居右。
*   **空間壓縮策略**：
    *   **訓練梯次資訊**：整合至單行（梯次、連隊、班別）。
    *   **環境設定**：整合至單行，「天候(晴天/雨天)」與「時段(日間/夜間)」並列，且圖文並茂。
    *   **射擊模式**：採用 2x2 網格佈局，按鈕與文字放大 1.3 倍以利點擊。
    *   **系統連線**：整理為單列水平顯示（攝影機A、攝影機B、追蹤程式）。
    *   **手動控制**：採用兩欄式精簡設計。

### 3. 倒靶模式更新
*   **命中即倒 (on_hit)**：命中目標立即執行倒靶。
*   **倒計時倒靶 (on_time)**：由使用者設定秒數，從開始射擊起算，時間到自動全線倒靶。
*   **發數倒靶 (on_count)**：個別射手打滿指定發數後自動倒靶。
*   **視覺反饋**：引入 CSS 3D 轉向效果（rotateY 90deg），擬真呈現靶板轉向動作。

### 4. 戰術演練自動化流程
*   **啟動序列**：播放訓練口令腳本，定時啟動計分與倒靶計時。
*   **終止序列與按鍵切換**：
    *   點選「停止射擊」後，執行完整語音腳本（停止射擊 -> 射手起立 -> 向右轉，下射擊線）。
    *   腳本執行完畢後，控制面板按鈕自動切換功能：
        *   左側按鈕：切換為藍色 **「上傳成績」**。
        *   右側按鍵：切換為綠色 **「重新畫面」**。
    *   **Excel 自動匯出**：點擊「上傳成績」時，系統會調用 `SheetJS` 自動產生並下載 `.xlsx` 格式的成績單，包含梯次、連隊、班別、編號、姓名、課目及中彈數。

### 5. 其他修正
*   移除介面版本號(樣式優化)與啟動警示彈窗。
*   刪除右下角冗餘的音效啟動按鈕。
*   保留並優化左側底部的陸軍官校校徽顯示。

### 6. 啟動腳本差異說明
| 腳本 | 用途 | 開啟的畫面 |
|------|------|------------|
| **`啟動系統.bat`** | 🔧 校準用 | `index.html` + `calibration_grid_a.html` + `calibration_grid_b.html` |
| **`啟動系統_自動化.bat`** | 🎯 正式訓練用 | `index.html` + `training_screen_a.html` + `training_screen_b.html` |

*   兩個腳本都會：
    1.  設定 PATH 到 `C:\msys64\mingw64\bin`（載入 DLL 依賴）
    2.  啟動 `cpp\t91_tracker.exe` 後端
    3.  開啟三個網頁視窗供手動拖曳至對應螢幕